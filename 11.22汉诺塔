#include<iostream>
using namespace std;

int hannuo(int n) {
    if(n == 1) {
        return 1;
    }
    if(n > 1) {
        return 2 * hannuo(n - 1) + 1;
    }
    return 0; 
}

int main() {
    int n;
    cin >> n;
    cout <<hannuo(n);
    return 0;
}
