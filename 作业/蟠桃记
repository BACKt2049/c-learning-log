#include<iostream>
#include<iomanip>
using namespace std;
int main()
{	
	int n; int sum = 1;
	while(1)
	{
		cin >> n;
		if (n != 0)
		{
			for (int i = 1; i < n; i++)
			{
				sum = (sum + 1) * 2;
			}
			cout << sum << endl;
			sum = 1;
		}
		else { break; }
	}
	return 0;
}
